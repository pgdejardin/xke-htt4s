library {
  db {
    url = "jdbc:postgresql://localhost:5432/postgres"
    user = "petstore"
    password = "secret"
    driver = "org.postgresql.Driver"
    connections = {
      poolSize = 10
    }
  }
  server {
    host = "0.0.0.0"
    port = 8080
  }
}
